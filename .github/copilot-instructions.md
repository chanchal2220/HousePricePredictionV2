# Copilot Instructions for House Price Prediction Project

## Project Overview
- This project is a single-file Python script for house price prediction.
- The main file is `House Price Prediction.py`. As of now, it is empty—add your code here.

## Key Conventions
- All code currently resides in a single script. If you add more files, keep related logic modular (e.g., data loading, preprocessing, modeling, evaluation).
- Use clear function and class definitions for each major step (e.g., `load_data()`, `train_model()`, `predict()`).
- Follow PEP8 for code style and naming.
- Use docstrings for all functions and classes.

## Dependencies
- Common libraries for this domain: `pandas`, `numpy`, `scikit-learn`, `matplotlib`, `seaborn`.
- If you add dependencies, document them in a `requirements.txt` file.

## Workflows
- Run the script directly: `python "House Price Prediction.py"`.
- For experiments, use Jupyter notebooks or split code into modules as the project grows.
- Add a `README.md` if you introduce new workflows or files.

## Data & Models
- Place data files in a dedicated `data/` directory if needed.
- Save trained models in a `models/` directory.
- Document any external data sources or model artifacts.

## Example Structure (if expanded)
```
House Price Prediction.py
requirements.txt
README.md
data/
models/
```

## AI Agent Guidance
- When generating new code, prefer modular, well-documented functions.
- If you add new files, update this instruction file and add a `README.md`.
- Reference this file for project-specific conventions.
